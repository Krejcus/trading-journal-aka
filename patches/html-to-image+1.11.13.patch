diff --git a/node_modules/html-to-image/es/util.js b/node_modules/html-to-image/es/util.js
index 90a3cd1..9817767 100644
--- a/node_modules/html-to-image/es/util.js
+++ b/node_modules/html-to-image/es/util.js
@@ -148,10 +148,26 @@ export function canvasToBlob(canvas, options = {}) {
 export function createImage(url) {
     return new Promise((resolve, reject) => {
         const img = new Image();
+        let settled = false;
+        const finish = () => {
+            if (settled)
+                return;
+            settled = true;
+            resolve(img);
+        };
         img.onload = () => {
-            img.decode().then(() => {
-                requestAnimationFrame(() => resolve(img));
-            });
+            // Chromium may reject decode() (EncodingError) for large SVG/foreignObject
+            // images and requestAnimationFrame never fires in a hidden tab. Neither
+            // must stall the capture forever: settle on whichever comes first.
+            const afterDecode = () => {
+                if (typeof requestAnimationFrame === 'function')
+                    requestAnimationFrame(finish);
+                setTimeout(finish, 120);
+            };
+            if (typeof img.decode === 'function')
+                img.decode().then(afterDecode, afterDecode);
+            else
+                afterDecode();
         };
         img.onerror = reject;
         img.crossOrigin = 'anonymous';
diff --git a/node_modules/html-to-image/lib/util.js b/node_modules/html-to-image/lib/util.js
index 6a6ae9c..deeb3f9 100644
--- a/node_modules/html-to-image/lib/util.js
+++ b/node_modules/html-to-image/lib/util.js
@@ -199,12 +199,28 @@ function canvasToBlob(canvas, options) {
 }
 exports.canvasToBlob = canvasToBlob;
 function createImage(url) {
-    return new Promise(function (resolve, reject) {
-        var img = new Image();
-        img.onload = function () {
-            img.decode().then(function () {
-                requestAnimationFrame(function () { return resolve(img); });
-            });
+    return new Promise((resolve, reject) => {
+        const img = new Image();
+        let settled = false;
+        const finish = () => {
+            if (settled)
+                return;
+            settled = true;
+            resolve(img);
+        };
+        img.onload = () => {
+            // Chromium may reject decode() (EncodingError) for large SVG/foreignObject
+            // images and requestAnimationFrame never fires in a hidden tab. Neither
+            // must stall the capture forever: settle on whichever comes first.
+            const afterDecode = () => {
+                if (typeof requestAnimationFrame === 'function')
+                    requestAnimationFrame(finish);
+                setTimeout(finish, 120);
+            };
+            if (typeof img.decode === 'function')
+                img.decode().then(afterDecode, afterDecode);
+            else
+                afterDecode();
         };
         img.onerror = reject;
         img.crossOrigin = 'anonymous';
