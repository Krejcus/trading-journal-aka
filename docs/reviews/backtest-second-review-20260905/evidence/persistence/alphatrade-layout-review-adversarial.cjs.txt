const esbuild = require('/Users/filipkrejca/Documents/trading-journal-aka/node_modules/esbuild');
const fs = require('node:fs');
const vm = require('node:vm');
const assert = require('node:assert/strict');
const root = '/Users/filipkrejca/Documents/trading-journal-aka';
const storage = new Map();
const source = fs.readFileSync(root + '/components/BacktestSessionsManager.tsx', 'utf8');
const savedRead = source.slice(source.indexOf('const SAVED_CHART_LAYOUT_KEY'), source.indexOf('const sanitizeSavedLayout'));
const exportRecord = source.slice(source.indexOf('const buildTradeRecord'), source.indexOf('const downloadJSON'));
const ctx = { window: { localStorage: { getItem: key => storage.get(key) ?? null } }, derivePositionSize: () => 1 };
vm.runInNewContext(esbuild.transformSync(savedRead + exportRecord + '\nglobalThis.audit={readSavedChartLayout,buildTradeRecord};', { loader: 'ts' }).code, ctx);
const built = esbuild.buildSync({ stdin: { contents: `export * from './services/chartWorkspaceDocument'; export * from './services/chartWorkspaceLayouts'; export { mergeChartSettings } from './services/chartSettings'; export { zonedTimeParts } from './services/chartTimeAxisFormat';`, resolveDir: root }, bundle: true, format: 'cjs', platform: 'node', write: false });
const pure = { module: { exports: {} }, structuredClone, setTimeout, clearTimeout };
vm.runInNewContext(built.outputFiles[0].text, pure);
const api = pure.module.exports;
const result = {};
const newKey = api.chartWorkspaceDocumentStorageKey('user-a');
storage.set(newKey, JSON.stringify({ format: 'alphatrade-workspace', version: 1, state: { layout: { name: 'new-saved-layout' } } }));
assert.equal(ctx.audit.readSavedChartLayout().layout, undefined);
result.newSaveSlot = { key: newKey, sessionCreatorFindsSavedLayout: false };
storage.set('alphatrade.candlekit.layout.alphatrade-market-workspace', JSON.stringify({ name: 'previous-user-legacy-layout' }));
assert.equal(ctx.audit.readSavedChartLayout().layout.name, 'previous-user-legacy-layout');
result.legacySlot = { currentUserASeesUnownedLegacy: true };
const exported = ctx.audit.buildTradeRecord({ id: 'trade-123', notes: 'new notes', tags: ['My setup'], setupType: 'reaction', autoConfluence: { htf: ['u PDH'], ltf: [] }, htfConfluence: ['u PDH', 'Manual'], ltfConfluence: [] });
assert.equal(exported.notes, 'new notes');
for (const field of ['id', 'tags', 'autoConfluence', 'setupType']) assert.equal(Object.hasOwn(exported, field), false);
result.aiJsonExport = { notesIncluded: true, fieldsMissing: ['id', 'tags', 'autoConfluence', 'setupType'] };
const layout = { version: 1, tree: api.buildChartWorkspaceLayout({ id: '1', component: 'alphatrade-chart', panelIdPrefix: 'chart-', panelTitle: n => `Chart ${n}`, configs: [{ root: 'MNQ', timeframe: '5m', showFvg: false, showLevels: true, showStructure: false }] }) };
const state = { layout, panels: { 'chart-1': { drawings: [], logicalRange: null, priceRange: null, autoScale: true, indicators: null } }, layoutId: '1', activePanelId: 'chart-1', syncSettings: { symbol: false, interval: false, crosshair: true, time: true, dateRange: false, drawings: true }, appearance: { chartSettings: { symbol: { timeZone: 'Mars/InvalidZone' } } } };
const parsed = api.parseChartWorkspaceDocument({ format: 'alphatrade-workspace', version: 1, state });
const settings = api.mergeChartSettings(parsed.document.state.appearance.chartSettings, true);
assert.throws(() => api.zonedTimeParts(1000, settings.symbol.timeZone), /Invalid time zone/);
result.importedAppearance = { parserAcceptsInvalidTimezone: true, existingChartFormatterThrows: true };
const nq = structuredClone(state); nq.layout.tree.layout.children[0].children[0].config.config.root = 'NQ';
assert.equal(api.parseChartWorkspaceDocument({ format: 'alphatrade-workspace', version: 1, state: nq }).kind, 'complete');
result.importRoot = { parserAcceptsNqWithoutSessionAllowlist: true };
console.log(JSON.stringify({ source: 'canonical pure functions and actual manager read/export helpers; synthetic values only, no network', ...result }, null, 2));
