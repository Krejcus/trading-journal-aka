/**
 * Public source labels for the two daily P&L read models shown around copier UI.
 * Keep them explicit: the copier ledger is not an account/broker statement.
 */
export const COPIER_LEADER_DAILY_STATS_LABEL = 'Leader · jen obchody přes kopírku · bez poplatků';
export const BROKER_ACCOUNTS_DAILY_PNL_LABEL = 'Účty (broker, vč. poplatků)';
